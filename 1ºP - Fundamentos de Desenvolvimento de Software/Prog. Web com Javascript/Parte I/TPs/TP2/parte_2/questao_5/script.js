const melhorGuitarrista = "Jimi Hendrix";
let a = 1, b = 2, c = 3;
let salarioMinimo = 1412;

/* 
LET são variáveis que futuramente podem ter o seu valor alterado 
CONST são variáveis constantes, em que o valor inserido na inicialização não pode ser alterado.

Exemplo:
let idade = 22;
idade++; // Pode acontecer :]

const idade = 22;
idade--; // não é permitido :/

*/

// Var são consideradas más práticas porque pode causar problemas de escopo, pode ser redeclarada e até usada antes de ser declarada.
// Já não declarar suas variáveis, faz com que todas elas sejam globais no código, dificultando a manutenção e poluindo o escopo.